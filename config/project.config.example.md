# Project Configuration Template

Copy these values into a private local file or the target repository's safe configuration documentation. Do not put secrets here.

```text
PROJECT_NAME=REPLACE_PROJECT_NAME
PROJECT_STACK=REPLACE_STACK
LOCAL_URL=REPLACE_LOCAL_URL
PRODUCTION_URL=REPLACE_PRODUCTION_URL
CLICKUP_WORKSPACE_NAME=REPLACE_CLICKUP_WORKSPACE_NAME
CLICKUP_WORKSPACE_ID=REPLACE_CLICKUP_WORKSPACE_ID
CLICKUP_SPACE_NAME=REPLACE_CLICKUP_SPACE_NAME
CLICKUP_LIST_NAME=REPLACE_CLICKUP_LIST_NAME
CLICKUP_LIST_ID=REPLACE_CLICKUP_LIST_ID
CLICKUP_ASSIGNEE_NAME=REPLACE_CLICKUP_ASSIGNEE
CLICKUP_ASSIGNEE_ID=REPLACE_CLICKUP_ASSIGNEE_ID
```
